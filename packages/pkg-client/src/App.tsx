import React from 'react';
import NumberView from './NumberView';

const App = () => {
  return (
    <div>
      <div>Mono Repo SSR with React, Redux Test...</div>
      <NumberView />
    </div>
  );
};

export default App;
